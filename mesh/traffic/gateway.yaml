apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: public-api
  namespace: platform
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - api.platform.local
