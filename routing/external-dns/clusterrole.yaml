apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: external-dns-istio-read
  labels:
    app.kubernetes.io/name: external-dns
    app.kubernetes.io/component: rbac
rules:
  - apiGroups:
      - networking.istio.io
    resources:
      - gateways
      - virtualservices
    verbs:
      - get
      - list
      - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: external-dns-istio-read
  labels:
    app.kubernetes.io/name: external-dns
    app.kubernetes.io/component: rbac
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: external-dns-istio-read
subjects:
  - kind: ServiceAccount
    name: external-dns
    namespace: external-dns
